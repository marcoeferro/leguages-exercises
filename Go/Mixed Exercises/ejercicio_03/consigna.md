### **3️⃣ Iteración con `for`**
**Ejercicio:**  
Escribe una función `ReverseString(s string) string` que reciba un string y lo retorne al revés.  

**Ejemplo:**  
```go
ReverseString("hello")  // Devuelve "olleh"
ReverseString("golang") // Devuelve "gnalog"
```